name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1        
    - name: Configuration
      run : export DOCKER_HOST=tcp://35.246.151.122:2375 
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker-compose -H "$DOCKER_HOST" -f docker-compose.yml up frontend
